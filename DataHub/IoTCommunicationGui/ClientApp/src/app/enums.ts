export enum SensorType {
  OnOffSensor = 0,
  Temperature = 1,
  Pressure = 2
}

export enum UnitType {
  Undefined  = 0,
  DegreeCelsius = 1,
  Pascals = 2
}

export enum ChartType {
  Undefined = 0,
  LineChart = 1
}
